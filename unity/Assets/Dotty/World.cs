using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class World : MonoBehaviour
{
    private Dotty.World _world; 
    
    public ObservedList<Attractor> attractors; 
    public ObservedList<Vortex> vorticies;
    public ObservedList<NoiseField> noiseFields;  
    

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
